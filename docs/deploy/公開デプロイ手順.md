# 公開デプロイ手順

最終更新: 2026年5月29日

## 目的

Custom GPT Actionsや簡易管理画面から呼べるように、`gen_api.py` をHTTPS公開URLへ置く。

## 現行方針

6/8確認では、Render上の常設APIを主ルートにする。

- Render URL: `https://gen-deck.onrender.com`
- OpenAPI schema: `https://gen-deck.onrender.com/openapi.json`
- Custom GPT Actionsに見せる操作は `generateSmokeDeck` と `generateDeckFromJsonQuery` の2つに絞る
- PPTX生成結果はChatGPT内添付ではなく、`download_url` 取得までを成功条件にする

ngrok一時公開は過去に検証済みだが、Custom GPT Actions側で `ClientResponseError` が残ったため、現在はフォールバック扱いにする。

## 追加した対応

- `Dockerfile`: Render Docker Web Service用
- `requirements.txt`: PPTX生成に必要なPython依存
- `render.yaml`: Render Blueprint用の最小設定
- `.dockerignore`: デプロイ不要な生成物と原本資料を除外
- `gen_api.py`: `PORT` / `HOST` / `PUBLIC_URL` 環境変数対応
- `gen_api.py`: `/openapi.json` が `X-Forwarded-Proto` と `Host` から公開URLを推定
- `gen_api.py`: 顧客素材なしの公開環境では安全なデモ背景を自動生成
- `slide_render.py`: macOSヒラギノがないLinux環境ではNoto CJKフォントへフォールバック

## ローカルDocker検証

2026-05-28にDocker Desktop 4.75.0で確認済み。`.dockerignore` で顧客素材と既存背景を除外した状態でも、デモ背景の自動生成でPPTX生成まで通る。

```bash
docker build -t gendeck-api:local .
docker run --rm -p 8791:8787 -e PORT=8787 -e HOST=0.0.0.0 gendeck-api:local
```

確認結果:

- `GET /health`: 成功
- `GET /openapi.json`: `X-Forwarded-Proto` と `Host` から公開URLを推定できる
- `GET /generate-smoke`: 成功。コンテナ内でPPTX生成まで完了
- Docker build contextは約40KBまで削減済み。顧客資料・背景・生成物は含めない

## Renderでの作成

1. GitHubにこのリポジトリをpushする
2. RenderでNew Web Serviceを作る
3. EnvironmentはDockerを選ぶ
4. Service nameは `gendeck-api` などにする
5. Free planでよい
6. デプロイ後、RenderのURLを開く

確認URL:

```text
https://{render-service}.onrender.com/health
https://{render-service}.onrender.com/openapi.json
https://{render-service}.onrender.com/generate-smoke
```

`/openapi.json` の `servers[0].url` がRenderのHTTPS URLになっていればOK。

## 2026-05-28 デプロイ結果

Render Web Serviceを作成済み。

```text
Service name: gen-deck
Service ID: srv-d8bljhu7r5hc738ol3bg
Branch: codex-render-deploy
URL: https://gen-deck.onrender.com
Schema: https://gen-deck.onrender.com/openapi.json
```

確認結果:

- `GET /health`: 成功
- `GET /openapi.json`: 成功。`servers[0].url` は `https://gen-deck.onrender.com`
- `GET /generate-smoke`: 成功。`ok: true` とPPTX metadataを返す

Render版schemaは `docs/api/action_openapi_render.json` に保存済み。

## 2026-05-29 管理画面デプロイ結果

無料管理画面をRenderへ反映済み。

```text
Branch: codex-render-deploy
Commit: 6103bc2 Add lightweight admin UI
Admin URL: https://gen-deck.onrender.com/admin
Alias: https://gen-deck.onrender.com/admin.html
Sample JSON: https://gen-deck.onrender.com/sample-deck.json
```

追加したAPI配信:

- `GET /admin`: 管理画面HTML
- `GET /admin.html`: 管理画面HTMLの別名
- `GET /sample-deck.json`: 公開安全なサンプルdeck JSON
- `GET /`: `admin_url` を含むサービスメタ情報

公開後の確認結果:

- `GET /admin`: 成功。管理画面HTMLを返す
- `GET /sample-deck.json`: 成功。`sample_deck` を返す
- `POST /generate`: 成功。`sample_deck.pptx` を生成
- `HEAD /files/sample_deck.pptx`: 成功。HTTP 200、PPTX content-type

管理画面の役割:

- ChatGPTまたはCustom GPTが作ったdeck JSONを読み込む
- 簡易バリデーションする
- `POST /generate` へ渡してPPTX化する
- `download_url` を開く

## ngrok一時公開フォールバック

ローカルAPIを一時的に外へ出す場合だけ使う。

```bash
ngrok http 8787
```

表示されたHTTPS URLを指定してAPIを起動する。

```bash
python3 gen_api.py --host 127.0.0.1 --port 8787 \
  --public-url https://xxxx.ngrok-free.app
```

確認:

```bash
curl http://127.0.0.1:8787/openapi.json
```

`servers[0].url` がngrok URLになっていればOK。

過去の確認結果:

- ngrok authtoken設定済み
- ngrok経由の `/health` / `/openapi.json` / `/generate` はcurlで成功
- Custom GPT Actionsではngrok無料ドメインまたはActions実行層側の問題で `ClientResponseError` が残った
- そのため、ngrokは6/8本線ではなく、ローカルcurl実演用の保険として扱う

## Custom GPT Actionsに貼るもの

Render公開後、次のURLをブラウザで開いて中身をコピーする。

```text
https://{render-service}.onrender.com/openapi.json
```

GPTのActions設定:

- Authentication: `None`
- Schema: 上記OpenAPI JSONを貼り付ける

## Previewで試す

まずは固定デッキで試す。

```text
generateSmokeDeckを呼んでください。
```

通ったら、次にGET版の `generateDeckFromJsonQuery` を試す。

```text
次のJSONを文字列として `deck_json` に入れて、generateDeckFromJsonQueryを呼んでください。

{
  "deck_title": "action_smoke_test",
  "color": "terracotta",
  "slides": [
    {
      "type": "statement",
      "color": "deep_green",
      "content": {
        "eyebrow": "Action test",
        "lines": ["接続テスト", "PPTX生成"]
      }
    }
  ]
}
```

## 成功条件

- Custom GPT Actionsから `generateSmokeDeck` または `generateDeckFromJsonQuery` が呼べる
- Render logsにリクエストが残る
- レスポンスに `ok: true` が返る
- `filename` / `relative_path` / `download_url` / `size_bytes` が返る
- GPT ActionsのAvailable actionsは `generateSmokeDeck` と `generateDeckFromJsonQuery` の2つだけにする

## それでも失敗する場合

Render logsにリクエストがない場合:

- Custom GPT Actions実行層
- GPT workspace/domain制限
- schema登録/モデル選択

Render logsにリクエストがあり、APIが500を返す場合:

- Render上のファイル不足
- フォント不足
- 背景プール不足
- PPTX生成例外

6/8のフォールバックは、Custom GPTが作ったdeck JSONをローカルAPIまたはcurlでPPTX化する。
